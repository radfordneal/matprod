#!/bin/bash

# Run piped-matprod tests with helpers, but not splitting operations.

for hlp in $@; do
  export HELPERS=$hlp
  unset SPLIT
  echo RUNNING TESTS WITH $HELPERS HELPERS, NOT SPLIT
  ../run-tests piped-matprod >& piped-matprod-hlp$HELPERS.out
  ../tim piped-matprod-hlp$HELPERS
done
